apiVersion: v1
kind: ServiceAccount
metadata:
  name: openexecutive
  labels:
    app.kubernetes.io/part-of: openexecutive
# GHCR packages are private by default. Create this image-pull secret before
# applying the kustomization; see k8s/README.md.
imagePullSecrets:
  - name: ghcr-pull
